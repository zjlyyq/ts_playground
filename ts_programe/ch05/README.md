## 前置
忽略配置文件，单文件编译，且检查报错，有报错不输出。
```sh
npx tsc 4.1.2.ts --ignoreConfig --noEmitOnError
```

## 类和继承

## super

## 以this为返回类型
`this`可以用作返回值，也可以用作类型。对类来说，`this`类型可以用于注解方法的返回类型。

[5.3.ts](./5.3.ts)

## 接口

类型别名和接口都是命名类型的方式。这之间有相同点也有区别。
主要有三大区别:
1. 接口右边必须是结构。
2. 接口进行扩展时，会对新接口是否可赋值给被扩展接口进行校验。
3. 接口可以进行声明合并，但类型别名如果重复会报错。

[5.4.0.ts](./5.4.0.ts)
### 声明合并


### 接口的实现
[5.4.2.ts](./5.4.2.ts)


### 实现接口还是扩展抽象类

接口更通用、更轻量，而抽象类作用更具体、功能更丰富。

接口不生成JavaScript代码，只存在于编译时。

## 类是结构化类型
与其他类型一样，TypeScript根据结构比较类，与名字无关。难道是鸭子类？
[5.5.ts](./5.5.ts)

## 类既声明值也声明类型

在TypeScript中，类型和值位于不同的命名空间中。根据场合， TypeScript知道你要使用的是类型还是值。

这种根据上下文进行解析的特性十分有用，可以做一些很酷的事情，例如实现伴生类型（Companion Type）。

类和枚举比较特殊，它们既在类型命名空间中生成类型，也在值命名空间中生成值。

## 多态

## 混入

## 装饰器
[5.9.ts](./5.9.ts)

## 模拟final类
构造函数配置为私有以后，只能通过静态方法创建实例，无法被继承扩展，也无法直接调用new运算符实例化类。。

[5.10.ts](./5.10.ts)

## 设计模式
### 工厂模式
工厂模式是创建某种类型的对象的一种方式，这种方式把创建哪种具体对象留给创建该对象的工厂决定。
[5.11.1.ts](./5.11.1.ts)
### 建造者模式
建造者模式把对象的建造方式与具体的实现方式区分开。
[5.11.2.ts](./5.11.2.ts)

## 练习
1. 类和接口有什么区别？
   > 1. 接口必须是接口。2. 接口会对扩展接口是否可赋值给被扩展接口进行校验。3. 接口可以进行声明合并。

2. 把类的构造方法标记为private后，不能实例化或扩展类。那么把构造方法标记为protected的呢？请在代码编辑器中试一试，看能不能找出答案。
   > 可以被扩展，但不能直接用new。
   
3. 在5.11节实现的基础上，牺牲一点抽象性，设法提升安全性。更新实现，让使用方在编译时知道，调用Shoe.create('boot')返回一个Boot实例，调用Shoe.create('balletFlat')返回一个 BalletFlat 实例，而不是都返回一个Shoe实例。提示：回想一下4.1.9节的内容。
   > 见[exercise.ts](./exercise.ts)
4. （有难度）思考如何设计对类型安全的建造者模式。在5.11.2节实现的基础上进行扩展，做到以下两点：
a. 在编译时确保不能在设置 URL 和请求方法之前调用.send。如果要求用户以特定顺序调用各方法，是不是更容易满足（提示：不返回this，应该返回什么）？
b.（更有难度）在保证前一点的基础上，如果想让用户以任意顺序调用其他方法，应该如何修改设计呢（提示：使用哪个 TypeScript特性可以在调用方法之后把方法的返回类型 “添加”到this 类型上）？
   > 见[exercise.ts](./exercise.ts) 做不出来
   > 标准答案 [exercise.ts](../submodules/programming-typescript-answers/src/ch05/exercises.ts)
