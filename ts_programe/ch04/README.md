## 前置
忽略配置文件，单文件编译，且检查报错，有报错不输出。
```sh
npx tsc 4.1.2.ts --ignoreConfig --noEmitOnError
```

## 话题
+ 在TypeScript中声明和调用函数的不同方式。
+ 签名重载
+ 多态函数
+ 多态类型别名


只有少数情况能根据上下文推导出参数的类型，详见4.1.8节。返回类型可以自己推导，推荐不写注解。

## 声明和调用函数
5种函数声明。

调用函数传参必须类型和数量符合函数定义。

### 可选和默认的参数

### 剩余参数

[4.1.2.ts](./4.1.2.ts)

### call, apply, bind

[4.1.3.ts](./4.1.3.ts)

> tsc标志：strictBindCallApply 的默认值为false，开启后，call, apply, bind 方法的参数类型必须与函数定义中的参数类型一致。默认值为false，不开启时，call, apply, bind 方法的参数类型可以与函数定义中的参数类型不一致。开启后，call, apply, bind 方法的参数类型必须与函数定义中的参数类型一致。使用 strict 模式默认开启。


### 注解this的类型
[4.1.4.ts](./4.1.4.ts)

> tsc标志：noImplicitThis 强制显示注解函数中类似的类型。

### 生成器函数
[4.1.5.ts](./4.1.5.ts)

更多生成器的知识，请访问MDN上的相关页面 。
https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Generator

### 迭代器
[4.1.6.ts](./4.1.6.ts)
迭代器是生成器的相对面。生成器是生成一系列值的方式，而迭代器是使用这些值的方式。

#### 可迭代对象

#### 迭代器

> tsc标志：downlevelIteration


更多迭代器的知识，请访问MDN上的相关页面 。
https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Guide/Iterators_and_generators

### 调用签名
[4.1.7.ts](./4.1.7.ts)

### 上下文类型推导
[4.1.8.ts](./4.1.8.ts)

[上下文类型推导.md](./上下文类型推导.md)

### 函数类型重载
[4.1.9.ts](./4.1.9.ts)

需要先明白调用签名有完整的和简写的。完整的才能够支持重载的写法。参见 [4.1.9.ts#L1-L6](./4.1.9.ts#L1-L6)

## 多态

### 什么时候绑定泛型？
声明泛型的位置不仅决定泛型的作用域，还决定TypeScript何时为泛型绑定具体的类型。
1. 适用于函数调用时绑定泛型类型。
2. 适用于类型别名时显示绑定泛型类型。
具体见 [4.2.1.ts](./4.2.1.ts)

[泛型何时被确定](./泛型何时被确定.md)

### 可以在什么地方声明泛型
只要是TypeScript支持的声明 **调用签名** 的方式，都可以在签名中加入泛型。
[4.2.2.ts](./4.2.2.ts)

### 泛型推导
[4.2.3.ts](./4.2.3.ts)

### 泛型别名
在类型别名中，只有一个地方可以使用泛型，即类型别名名称之后，赋值运算符（=）之前。
[4.2.4.ts](./4.2.4.ts)

### 受限的多态

#### 多个约束的受限的多态

#### 使用受限的多态模拟变长参数
[4.2.5.ts](./4.2.5.ts)



## 类型驱动开发
先草拟**类型签名**，然后填充值的编程风格。

## 练习
1. TypeScript 能从函数的类型签名中推导出哪些部分的类型，参数、返回值，还是二者都可以？
2. JavaScript 的 arguments 对象是类型安全的吗？如果不是，我们可以采取什么措施？
3. 假如你想预定立即开始的旅行。更新本章前面重载的 reserve 函数（见 4.1.9 节），添加第三个调用签名。这个签名只有目的地，没有开始日期。更新 reserve 的实现，支持这个新增的签名。
4. （有难度）更新本章前面实现的 call 函数（使用受限的多态模拟变长参数），让它只支持第二个参数为字符串的函数。如果传入除此以外的函数，在编译时报错。
5. 实现一个类型安全的小型断言库 is。先草拟类型。实现之后，可以像下面这样使用：

```ts
// 字符串与字符串比较
is('string', 'otherstring') // false
// 布尔值与布尔值比较
is(true, false) // false
// 数字与数字比较
is(42, 42) // true
// 比较两个不同类型的值应该抛出编译时错误
is(10, 'foo') // Error TS2345: Argument of type '"foo"' is not assignable
// to parameter of type 'number'.
// （有难度）可以传入任意个参数
is([1], [1, 2], [1, 2, 3]) // false
```
[4.5.ts](./4.5.ts)
## 疑惑点


